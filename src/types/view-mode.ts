/**
 * ViewMode defines the available layout options for presenting offers.
 *
 * @typedef {"grid" | "list"} ViewMode
 * @property "grid" Renders items in a multi-column grid format.
 * @property "list" Renders items in a single-column list format.
 */
export type ViewMode = "grid" | "list";
